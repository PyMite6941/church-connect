# Optional API backend. When unset, church-connect runs on the localStorage
# demo adapter so it works with zero infrastructure.
VITE_API_BASE_URL=
# Force a tenant id regardless of subdomain (useful for local dev / single-church installs)
VITE_DEFAULT_TENANT=grace-community
